import type { UploadCardLocale } from '../interface';

// Merge into a locale object
const locale: UploadCardLocale = {
  placeholder: '上传',
  dragPlaceholder: '拖拽改变位置',
  removePlaceholder: '删除',
  seePlaceholder: '查看',
};

export default locale;
