import type { UploadNameLocale } from '../interface';

// Merge into a locale object
const locale: UploadNameLocale = {
  placeholder: '上传',
  removePlaceholder: '删除',
  seePlaceholder: '查看',
};

export default locale;
