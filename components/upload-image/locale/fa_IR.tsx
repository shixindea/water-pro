import type { UploadImageLocale } from '../interface';

// Merge into a locale object
const locale: UploadImageLocale = {
  placeholder: 'Upload',
  removePlaceholder: 'Remove',
  seePlaceholder: 'See',
};

export default locale;
