import { withInstall } from '../_util/type';
import PreviewImage from './preview-image';

export { PreviewImage };
export { previewImageProps } from './props';
export default withInstall(PreviewImage);
